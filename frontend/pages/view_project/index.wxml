<view class="page">
    <view class="page__hd">
        <view class="page__title" wx:if="{{searchbar==true}}">
            欢迎回来！{{username}}
        </view>
        <ul >
            <view wx:for="{{projects}}" class="Vbox projectBox">
                <view>{{item.name}}</view>
                <view class="text_gray">描述：{{item.description}}\n</view>
                
            </view>
            <!--<view class ="cutline"></view>-->
        </ul>
    </view>
</view>