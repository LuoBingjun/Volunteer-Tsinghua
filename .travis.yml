sudo: false

language: node_js

node_js:
  - stable

cache:
  directories:
    - node_modules

before_script:
   - npm install -g gulp

script: cd frontend && npm install --save-dev gulp && npm run watch
  